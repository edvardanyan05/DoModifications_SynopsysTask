#pramaonce

#include <iostream>
#include <string>
using namespace std;

bool isValidSTR(const string &);
bool isDigit(const char);
string create_correct_string(string &);
long long toDigit(const string &);
long long calculate_simpleSTR(const string &s);
